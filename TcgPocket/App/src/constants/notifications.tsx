import { NotificationProps } from '@mantine/notifications';

export const tcgNotifications = {
  signIn: {
    title: 'Sign-in successful!',
    autoClose: 5000,
  } as NotificationProps,
  register: {
    title: 'User registered!',
    autoClose: 5000,
  } as NotificationProps,
};
